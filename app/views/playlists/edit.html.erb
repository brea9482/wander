<% if user_signed_in? %>
  <div class="well">
    <h1>Edit your playlist</h1>

    <%= @playlist.errors.full_messages.first if @playlist.errors.any? %>

    <%= render "edit_form" %>
  </div>
<% end %>
